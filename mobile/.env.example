# Copy this file to .env and fill in real values.
# Expo only exposes variables prefixed with EXPO_PUBLIC_ to app code.

EXPO_PUBLIC_API_URL=http://localhost:8081

# Required for Google map tiles in a custom Android or iOS build. Restrict the
# key to this application's Android package/SHA-1 and iOS bundle identifier.
GOOGLE_MAPS_API_KEY=
